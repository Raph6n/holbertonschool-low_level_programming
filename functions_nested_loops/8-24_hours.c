#include "main.h"

void jack_bauer(void)
{
  for ( number >= 0; number <=0; number++);
for (number >= 0; number <=0; number++);
_putchar (':');
	   for (number >=0; number <=0; number ++);
for (number >=0; number <=0; number ++);
return 0
  }
