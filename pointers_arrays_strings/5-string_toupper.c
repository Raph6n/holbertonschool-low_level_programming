#include "main.h"
#include <stdio.h>

/**
 * *string_toupper - transform lowercase to uppercase
 * Return: 0
 * 
 */

char *string_toupper(char *)

{
int a;

for (i = 0; str[a] != '\0'; i++)
{
if (str[a] >= 'a' && str[a] <= 'z')
{
str[a] = str[a] - ('a' - 'A');
}
}
}
