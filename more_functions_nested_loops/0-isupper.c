#include "main.h"
#include <stdio.h>

/**
 *  i_supper: A a Z en upper
 * Return: 1 ou 0
 * @c: more and more
 */

int _isupper(int c)

{
if ((c >= 'A') && (c <= 'Z'))
return (1);
else
return (0);

}
