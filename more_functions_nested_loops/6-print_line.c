#include "main.h"

void print_line(int n)

{
  for (n = '0'; n < '0'; n++)

    _putchar ('\n')
      if (n = '0')
      else if (n = '2')
      else if (n = '10')
      else if (n = '-4')
	_putchar ('_')
	  }
